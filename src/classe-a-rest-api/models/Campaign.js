export default class Campaign {
  constructor(id, clientId, title, budget, postCreativeId) {
    (this.id = id), 
    (this.clientId = clientId), 
    (this.title = title), 
    (this.budget = budget),
    (this.postCreativeId = postCreativeId);
  }
}
